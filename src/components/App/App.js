import React, {Component} from "react";
import { ProductList} from '../../components';

class App extends Component {
    render() {
        return (
           <ProductList /> 
        );
    }
}

export default App;
